:css

.grid_8
  %h1.title.tk-bree Listing users

  %table.list
    %tr
      %th.left ID
      %th.left Email
      %th #Events
      %th Unlimited

    - @users.each do |user|
      %tr
        %td
          = user.id
        %td
          = user.email
        %td.center
          = user.events.length
        %td.center
          %input.user_field_toggle{:type => 'checkbox', :name=> 'is_unlimited', :checked => ('checked' if user.is_unlimited?), :rel => user_path(user.id)}

