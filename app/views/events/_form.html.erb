<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>
    <ul>
    <% @event.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :not_before %><br />
    <%= f.date_select :not_before %>
  </div>
  <div class="field">
    <%= f.label :not_after %><br />
    <%= f.date_select :not_after %>
  </div>
  <div class="field">
    <%= f.label :admin_password %><br />
    <%= f.text_field :admin_password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
